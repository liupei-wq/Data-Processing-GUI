把資料夾傳給對方後，對方只需要做一次：

Windows：

安裝 Python（勾選「Add Python to PATH」）
在資料夾裡開 cmd，執行 pip install -r requirements.txt
之後每次雙擊 啟動_Windows.bat 即可
Mac：

安裝 Python（通常已內建，或用 Homebrew）
在終端機進入資料夾，執行 pip3 install -r requirements.txt
執行一次 chmod +x 啟動_Mac.sh
之後每次雙擊 啟動_Mac.sh 即可