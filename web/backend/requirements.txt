fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
aiofiles>=23.2.1
pydantic>=2.7.0
numpy>=1.26.0
pandas>=2.0.0
openpyxl>=3.1.0
xlrd>=2.0.1
scipy>=1.12.0
lmfit>=1.3.0
# xraylarch>=2026.2.0
# 生產環境（Render）不安裝 xraylarch：依賴樹過大（含 wxpython/jupyter/mp-api/pymatgen/silx 共 ~1.5GB），
# Docker build 會在下載 larixite 時 broken pipe，且 wxpython 在 headless server 上沒用。
# 後端 routers/xas.py 的 _calculate_exafs_larch() 已有 lazy import 與 fallback：
# 沒裝 Larch 時 EXAFS 自動降到 SciPy preview（前端會顯示實際走的 method）。
# 本地開發要 Larch autobk/xftf：手動 `pip install xraylarch` 即可。
